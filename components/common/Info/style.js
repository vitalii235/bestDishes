const {StyleSheet} = require('react-native');

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
  },
  renderList:{
    marginTop:65,
    zIndex:1,
  },
  backgroundImg: {flex: 1, resizeMode: 'cover'},
});
